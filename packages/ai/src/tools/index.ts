/**
 * Registro das tools da agenda para o bot (Fase 5). Preenchido no Grupo 8 do
 * tasks.md, importando o contrato de `@blademidia/core` (`agendaTools`) e
 * expondo-o no formato de tool-use da Claude API. Sem execução em runtime nesta
 * fase — é só o contrato.
 */
export const AI_TOOLS_READY = false;
