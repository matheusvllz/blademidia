# Next.js espera um node_modules plano (não os symlinks aninhados do pnpm) —
# sem isto, o dev não compila o Tailwind e `next start` falha com
# "Cannot find module ./vendor-chunks/...". node-linker=hoisted resolve ambos.
node-linker=hoisted
